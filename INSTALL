./configure
make
Optionally: make check
make install

See `./configure --help` for more options.

More than likely you will need to specify the path
to your boost installation using --with-boost=<path>.
Also, if you installed curl in a non-standard location
you may need to indicate that to configure as well.
